all: items

items:
	gcc -o prog -g item.c mapElem_items.c mapArrayList.c main_items.c 

counter:
	gcc -o prog -g mapElem.c mapArrayList.c main.c 

linked:
	gcc -o prog -g mapElem.c mapLinkedList.c main.c

clean:
	rm -f prog

